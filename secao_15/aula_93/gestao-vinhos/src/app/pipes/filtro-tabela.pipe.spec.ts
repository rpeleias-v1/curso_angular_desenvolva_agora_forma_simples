import { FiltroTabelaPipe } from './filtro-tabela.pipe';

describe('FiltroTabelaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTabelaPipe();
    expect(pipe).toBeTruthy();
  });
});
