import { DestacarEstiloDirective } from './destacar-estilo.directive';

describe('DestacarEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
