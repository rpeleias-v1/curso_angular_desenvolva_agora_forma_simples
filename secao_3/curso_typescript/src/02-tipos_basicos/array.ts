let nomes: string[] = ['Rodrigo', 'Peleias', 'Programador Fullstack'];

nomes.forEach((nome: string) => {
    console.log(nome);
});

let cores: Array<any> = ['Amarelo', 'Vermelho', 'Azul'];
cores.push(10);
cores.push(false);
cores.forEach((cor: string) => {
    console.log(cor);
});